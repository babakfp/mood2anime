export const load = async ({ fetch, params }) => {
    // url.searchParams.append("genre", )
    // const res = await fetch("https://api.jikan.moe/v4/genres/anime")
    // const genres = await res.json()

    // return { genres }
    return {}
}
